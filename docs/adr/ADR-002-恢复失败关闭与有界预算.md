# ADR-002：恢复失败关闭与有界预算

- **状态**：Accepted
- **决策**：REPEAT、半帧缺口、可见性超时、decoder restart 失败和传输错误均阻止控制输出；恢复预算只有在新鲜完整 IDR 到达后重置。
- **原因**：会话重建本身不代表预测参考链恢复；在重建时重置预算会形成无限恢复循环。
- **后果**：超过预算进入 `Exhausted/RecoveryRequired`，需要上层显式干预；不自动放行旧检测。
