# ADR-003：组合根与外部适配器边界

- **状态**：Accepted
- **决策**：Server、Host 和 APP 各自只有明确组合根；外部网络、数据库、文件系统、socket、decoder 和 recovery 通过端口接入。
- **原因**：路由/Activity 直接构造适配器会造成反向依赖、不可测事务和锁重入。
- **后果**：Host 外部发送不持有状态锁；Server 远程调用不进入支付事务；APP 端口异常转为 fail-closed 快照。
