# ADR-001：跨语言流身份与线协议

- **状态**：Accepted
- **决策**：使用 `stream_epoch:uint63-positive + frame_sequence:uint32` 作为全局帧身份；v2 头固定 20 字节、网络字节序。
- **原因**：单独 sequence 在进程重启和回绕后会碰撞；Java 没有无符号 long，因此 C++ epoch 上限必须与正 `long` 对齐。
- **后果**：sequence 用尽前必须持久化预留更高 epoch；Host/APP 常量由独立审计脚本交叉检查。
